import { KnowledgeImportancePipe } from './knowledge-importance.pipe';

describe('KnowledgeImportancePipe', () => {
  it('create an instance', () => {
    const pipe = new KnowledgeImportancePipe();
    expect(pipe).toBeTruthy();
  });
});
