import { KnowledgeLevelPipe } from './knowledge-level.pipe';

describe('KnowledgeLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new KnowledgeLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
